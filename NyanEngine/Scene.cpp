#include "Scene.h"
#include "stdafx.h"

bool InitScene(){
	Scene scene;
	// TODO: Вектор сцен, количество которых берем из конфига
	/*vector<scene> VectorScene(countscene);
	for(size_t i=0;i<countscene;++i){
		scene *temp = new scene;
		VectorScene.push_back(temp);
	}*/
	return true;
}
